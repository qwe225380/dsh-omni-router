name: Omni Router
description: Auto-routes tasks by complexity, thinking mode, risk, and task type — simple tasks run directly, complex/risky tasks enter plan mode with structured plans and approval. Reuses installed capabilities when available.
order: 6
